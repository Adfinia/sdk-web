// Single source of truth for the library identifier emitted in every
// event's `context.library` block. Kept in sync with package.json by the
// release script (TODO — for now bump manually on release).

export const LIBRARY_NAME = 'adfinia-sdk-web'
export const LIBRARY_VERSION = '0.1.0'
